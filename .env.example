# Optional: Google Search Console HTML tag verification.
# In GSC → Add property → URL prefix → HTML tag → copy the content value only.
# Set in Vercel: Project → Settings → Environment Variables → PUBLIC_GOOGLE_SITE_VERIFICATION
# PUBLIC_GOOGLE_SITE_VERIFICATION=your_verification_token_here
